<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.weicong</groupId>
		<artifactId>corn-uas</artifactId>
		<version>0.0.1</version>
	</parent>
	<artifactId>corn-uas-biz</artifactId>
	<description>corn用户权限系统（提供用户管理、认证授权服务，ps：鉴权逻辑在 auth jar 包中）</description>

	<properties>
		<security.oauth.version>2.3.5.RELEASE</security.oauth.version>
	</properties>
	

	<dependencies>
		<dependency>
			<groupId>org.weicong</groupId>
			<artifactId>corn-common-base</artifactId>
			<version>0.0.1</version>
		</dependency>
		<dependency>
			<groupId>org.weicong</groupId>
			<artifactId>corn-common-auth</artifactId>
			<version>0.0.1</version>
			<exclusions>
				<!-- MARKE weicong 待解决 直接用auth jar包中的security依赖，redis会有问题，先排除这些依赖 -->
				<exclusion>
					<groupId>org.springframework.cloud</groupId>
					<artifactId>spring-cloud-starter-security</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.springframework.security.oauth</groupId>
					<artifactId>spring-security-oauth2</artifactId>
				</exclusion>
				<!-- MARKE weicong base jar包中有这些依赖，排除掉 -->
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-data-redis</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.apache.commons</groupId>
					<artifactId>commons-pool2</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>
		<!-- spring security start-->
		<!--spring security 、oauth依赖 -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-security</artifactId>
			<exclusions>
				<!-- 旧版本 redis操作有问题 -->
				<exclusion>
					<groupId>org.springframework.security.oauth</groupId>
					<artifactId>spring-security-oauth2</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.springframework.security.oauth</groupId>
			<artifactId>spring-security-oauth2</artifactId>
			<version>${security.oauth.version}</version><!--$NO-MVN-MAN-VER$-->
		</dependency>
		<!-- spring security end-->
	</dependencies>

</project>